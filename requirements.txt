# 实验二依赖包列表
# 环境要求：Python 3.11+, Linux (Ubuntu), NVIDIA GPU with CUDA

# 核心科学计算
numpy>=2.3.0                # 注意：需要 >=2.3.0 以支持 np.load 对象数组特性
pandas>=1.3.0
scipy>=1.7.0

# 单细胞分析
scanpy>=1.9.0               # 用于单细胞数据读取、预处理和分析
anndata>=0.8.0

# 深度学习（GPU版本）
torch>=2.0.0                # 用于构建和训练所有深度学习模型
torchvision>=0.15.0
torchaudio>=2.0.0
# 安装命令:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# 模型解释
captum>=0.6.0               # 用于实现集成梯度等模型可解释性方法

# 机器学习
scikit-learn>=1.0.0         # 用于评估指标计算（F1分数、ARI、NMI）和交叉验证
imbalanced-learn>=0.9.0

# 可视化
matplotlib>=3.5.0           # 用于数据和结果的可视化
seaborn>=0.12.0

# 进度条
tqdm>=4.62.0

# 其他工具
h5py>=3.7.0
leidenalg>=0.9.0
python-igraph>=0.10.0
